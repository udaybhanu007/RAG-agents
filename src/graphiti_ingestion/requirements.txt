# Graphiti Ingestion Requirements
# Phase 1 Implementation - Document Ingestion Only

# Core Graphiti dependencies
graphiti-core==0.8.1
openai>=1.0.0
neo4j>=5.0.0
pandas>=2.0.0

# Azure dependencies for blob storage
azure-storage-blob>=12.19.0
azure-core>=1.29.0
azure-identity>=1.15.0

# Configuration and validation
pydantic>=2.5.0
python-dotenv>=1.0.0

# Async support
asyncio>=3.4.3

# Logging and monitoring
structlog>=23.2.0

# Type hints and validation
typing-extensions>=4.8.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
mypy>=1.7.0

# Additional utilities
pathlib2>=2.3.7
# Note: mimetypes is a built-in Python module, not a pip package
